services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ingestion
      POSTGRES_PASSWORD: ingestion
      POSTGRES_DB: ingestion
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ingestion"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql+psycopg2://ingestion:ingestion@db:5432/ingestion
      # Optional: override the public reqres.in demo key
      REQRES_API_KEY: reqres-free-v1
    depends_on:
      db:
        condition: service_healthy
